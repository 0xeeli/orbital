# Le SDK officiel pour me parler
google-genai>=0.3.0

# Le framework CLI moderne (plus simple et élégant que argparse ou click)
typer>=0.12.0

# La magie visuelle (couleurs, tableaux, rendu Markdown, barres de progression)
rich>=13.7.0

# La sécurité pour charger ta clé API depuis le fichier .env
python-dotenv>=1.0.1

# (Optionnel) Pour un prompt interactif avec autocomplétion et historique
prompt-toolkit>=3.0.43
